# X Bookmark to Drive Saver - 実装計画

## プロジェクト概要
Xのブックマークした投稿をGoogle Driveに保存し、NotebookLMで活用できる形式でエクスポートするChrome拡張機能

## 完了した実装

### 1. 基本構造 ✅
- `manifest.json`: Chrome拡張の設定ファイル
  - Manifest V3形式
  - 必要な権限（storage, identity, Google APIs）
  - コンテンツスクリプトの設定

### 2. UI層 ✅
- `popup.html`: ポップアップインターフェース
  - Google認証ボタン
  - ブックマーク収集ボタン
  - エクスポート設定（形式選択、オプション）
  - プログレスバー
  - ログ表示

- `styles.css`: スタイルシート
  - モダンなUI/UX
  - レスポンシブデザイン
  - 視覚的フィードバック

- `popup.js`: UI制御ロジック
  - イベントハンドラー
  - データフォーマット機能（Markdown, JSON, Text）
  - プログレス管理

### 3. データ収集 ✅
- `content.js`: Xページからのデータ抽出
  - ブックマークページのスクレイピング
  - ツイートデータ抽出（テキスト、画像、動画、メタデータ）
  - スレッド展開機能
  - 自動スクロール＆ページネーション

## 残りの実装タスク

### 4. バックグラウンド処理（重要）
**ファイル**: `background.js`

**実装内容**:
- **Google OAuth 2.0認証**
  - `chrome.identity.getAuthToken()` を使用
  - アクセストークンの取得と保存
  - トークンのリフレッシュ処理

- **Google Drive API連携**
  - Files.create APIでファイルアップロード
  - マルチパートアップロード対応
  - エラーハンドリング（ネットワークエラー、認証エラー）
  - リトライロジック

- **メッセージハンドリング**
  - popup.js, content.jsからのメッセージ受信
  - 非同期処理の管理

**技術的考慮事項**:
- Service Worker形式（Manifest V3）
- CORS対応
- レート制限の考慮

### 5. アイコン画像
**ディレクトリ**: `icons/`

**必要なファイル**:
- `icon16.png` (16x16px)
- `icon48.png` (48x48px)
- `icon128.png` (128x128px)

**実装方法**:
1. シンプルなSVGアイコンを作成
2. ImageMagickまたはオンラインツールで各サイズに変換
3. または、プレースホルダー画像を生成

**デザイン案**:
- Xのロゴ + ブックマークアイコン + Driveアイコン
- カラー: #1DA1F2（X Blue）をベースに

### 6. ドキュメント
**ファイル**: `README.md`

**記載内容**:
1. **概要**: プロジェクトの目的と機能
2. **機能一覧**:
   - ブックマークの自動収集
   - 画像・動画・スレッドの取得
   - 複数形式でのエクスポート
   - Google Drive連携
   - NotebookLM対応

3. **セットアップ手順**:
   - Chrome拡張のインストール方法
   - Google Cloud Projectの設定
   - OAuth 2.0クライアントIDの取得
   - manifest.jsonへの設定

4. **使い方**:
   - Xのブックマークページを開く
   - 拡張機能を起動
   - Google認証
   - ブックマーク収集
   - エクスポート

5. **NotebookLMでの活用方法**:
   - エクスポートしたファイルのアップロード手順
   - 推奨形式（Markdown）
   - 活用例

6. **トラブルシューティング**:
   - よくある問題と解決方法

7. **技術スタック**:
   - Chrome Extension Manifest V3
   - Google Drive API v3
   - Vanilla JavaScript

8. **ライセンス**: MITライセンス

### 7. Google Cloud設定ガイド
**ファイル**: `GOOGLE_SETUP.md`

**記載内容**:
- Google Cloud Consoleでのプロジェクト作成
- Drive APIの有効化
- OAuth同意画面の設定
- クライアントIDの作成
- リダイレクトURIの設定

### 8. テストとデバッグ
**実装内容**:
- Chrome拡張のローカルテスト手順
- デバッグ方法（DevTools Console）
- エラーハンドリングの確認
- エッジケースのテスト:
  - ブックマークが0件の場合
  - 大量のブックマーク（100+）
  - 画像のみ/テキストのみの投稿
  - 非常に長いスレッド
  - ネットワークエラー

## 実装の優先順位

### 最優先（機能の核心）
1. **background.js** - Google Drive連携なしでは機能しない
2. **アイコン画像** - 拡張機能のロードに必要

### 高優先（ユーザー体験）
3. **README.md** - セットアップに必須

### 中優先（補助的）
4. **GOOGLE_SETUP.md** - 詳細な手順書
5. **テストとデバッグ** - 品質保証

## 技術的な課題と解決策

### 課題1: Xの動的コンテンツ
**問題**: Xは動的にコンテンツをロードするため、すべてのブックマークを取得するにはスクロールが必要

**解決策**:
- content.jsで実装済み
- 自動スクロール + 待機ロジック
- 重複検出（data-processed属性）

### 課題2: スレッドの完全取得
**問題**: 「非表示のリプライ」を含むスレッド全体を取得するのは困難

**解決策**:
- 現在の実装: 表示されているスレッドのみ取得
- 将来の改善: ツイート詳細ページを開いて完全なスレッドを取得
- または: Twitter API使用（認証が必要）

### 課題3: 画像の保存方法
**問題**: 画像URLをテキストで保存するだけでは、後で画像が削除される可能性

**解決策**（現在の実装）:
- 画像URLをMarkdown/JSONに含める
- NotebookLMはURL参照に対応

**将来の改善**:
- 画像をBase64エンコードしてファイルに埋め込む
- または、画像を別途Google Driveにアップロード

### 課題4: Google OAuth認証
**問題**: ユーザーごとにGoogle Cloud Projectの設定が必要

**解決策**:
- README.mdに詳細な手順を記載
- GOOGLE_SETUP.mdで画像付きガイド提供
- 将来: 公開拡張機能としてリリース（共通のClient ID使用）

## NotebookLM対応について

### 推奨形式: Markdown
**理由**:
- NotebookLMが最も読みやすい
- 構造化されたデータ
- 画像リンクのサポート
- 人間にも読みやすい

### 構造例:
```markdown
# X Bookmarks Export

## 1. ユーザー名 (@handle)
投稿日時: 2024-01-01

投稿本文...

### 画像
![画像1](url)

### スレッド
1. 返信1
2. 返信2

---
```

### JSON形式のメリット:
- プログラムでの処理が容易
- すべてのメタデータを保持
- NotebookLMも対応

## 次のステップ

1. ✅ ユーザーの承認を得る
2. background.jsの実装
3. アイコン画像の作成
4. README.mdの作成
5. テスト
6. Git commit & push

## 推定作業時間
- background.js: 30-45分
- アイコン: 10-15分
- README.md: 20-30分
- テスト: 15-20分
- **合計**: 約75-110分

## リスクと対策

### リスク1: Xの仕様変更
- **影響**: セレクターが変更されてデータ取得不可
- **対策**: 複数のセレクター候補を用意、定期的なメンテナンス

### リスク2: Google API制限
- **影響**: アップロード失敗
- **対策**: エラーハンドリング、リトライ、ローカル保存オプション

### リスク3: 大量データのパフォーマンス
- **影響**: ブラウザがクラッシュ
- **対策**: バッチ処理、最大件数制限、進捗保存
